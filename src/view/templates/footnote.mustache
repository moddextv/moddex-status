<p class="footnote">
	Checked every {{ seconds }} seconds, last run {{ lastRun }}.<br>
	Each bar is one hour. Hover or tap for its downtime, estimated from the checks that failed. Incident times are exact.<br>
	<span class="aside">History is kept for {{ retainDays }} days. </span>Machine readable at <code>/api/status</code>.
</p>
